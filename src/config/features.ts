// Feature flags — flip to re-enable gated sections with no other code changes.
export const FEATURES = {
  prompts: false, // Set true to restore Prompt Store sidebar, routes, and Home aggregations.
} as const;
